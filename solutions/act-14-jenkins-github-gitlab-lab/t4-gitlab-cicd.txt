1. Full contents of .gitlab-ci.yml (root of GitLab repository)

stages:
	- build

build_docker_image:
	stage: build
	image: docker:24.0.7
	services:
		- docker:24.0.7-dind
	variables:
		DOCKER_TLS_CERTDIR: ""
	script:
		- echo "$DOCKERHUB_TOKEN" | docker login -u "$DOCKERHUB_USERNAME" --password-stdin
		- docker build -t "$DOCKER_IMAGE:latest" .
	only:
		- main

2. GitLab CI/CD variables configured (names only)

- DOCKERHUB_USERNAME
- DOCKERHUB_TOKEN
- DOCKER_IMAGE

3. Evidence of pipeline run from GitLab Pipelines view (status and job name)

- Pipeline status: passed
- Job name: build_docker_image
- Example evidence line to paste from UI: Pipeline #1 for branch main finished with status passed; job build_docker_image passed.


